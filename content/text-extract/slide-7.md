# Core Concepts and Terms

Nodes Worker machines capable of running pods

Pods A group of one or more containers that is lifecycle managed

Services Network access to a resource, e.g. pod or port. Typically load balanced

Namespaces Logical separation for resources

Deployments Manages a replicated application, via a set of pods

Config Maps / Secrets Configuration for your workloads and apps
